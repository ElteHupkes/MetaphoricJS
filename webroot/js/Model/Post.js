/**
 * Post model / object
 *
 * @author Elte Hupkes
 */
App.Post = Em.Object.extend();
App.Post.reopenClass({
	/**
	 * Finds a post by slug
	 * @param slug
	 */
	find: function(slug) {

	},

	/**
	 * Returns a list of all posts for
	 * the index page.
	 */
	findAll: function() {

	}
});